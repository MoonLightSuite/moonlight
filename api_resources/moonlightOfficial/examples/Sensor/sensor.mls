monitor SensorNetwork {
                signal { int nodeType; real battery; real temperature; }
             	space {
             	edges { int hop; real weight; }
             	}
             	domain boolean;
             	formula everywhere [0.0, 1.0] #[ (battery - 0.5)>0 ]# & #[(temperature - 20)>0]#;
             }